--- event driven configs ---
-- local status = load_variant_humane("kicad", "events")
-- if status ~= 1 then 
--   hl.notification.create({ text = "events/kicad.lua failed to load!!", timeout = 4000, icon = "error" })
-- end

